const nome = "NomeExemplo"; //string
const numero1 = 1; //number
const numero2 = 2; //number
let nomePlace; //undefined - náo aponta lugar na memória
let exemplo = null; //nulo - náo aponta lugar na memória
const aprovado = false; //booleano - verdadeiro ou falso

/* Operadores Aritméticos
(), **, *, /, %, + e - */

/* Operadores de incremento
++ e -- (mais usados)
*/

/* Operadores de atribuição 
+=, -=, *=, /=, **=, %=, entre outros
*/