// Escreva uma função que recebe 2 números e retorne o maior deles
const max2 = (x, y) => x > y ? x : y;     // arrow function com função ternária 
console.log(max2(10, 5));

/*
function max (x, y) {
    if (x > y) {        condição
        return x;
    } else {
        return y
    }
}
console.log(max(1, 2))  // atribui um valor para 'x' e um para 'y'
*/